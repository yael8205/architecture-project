export interface GiftInOrderDto {
    id: number;        
    giftId: number;         
    giftName: string;       
    giftPictureUrl: string;  
    giftCardPrice: string;   
    isWinner: boolean;       
}